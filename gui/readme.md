Folder and branch (branchGuiTeam2) now just for Timur, Artem and Kostya
/counter - example easy GuiApp with most important thing in PyQt

requirements.txt - file with library and his version (add anything library if y use it)
use "pip install -r requiremets.txt" for install all uses libraries 

advice: use local venv on ur computers and 
install libraries in venv (and write ur libraries in requirements.txt)